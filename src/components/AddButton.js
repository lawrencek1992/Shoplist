import React from 'react';

// write a function to open the modal when you click the + button

const AddButton = () => {
    return (
        // Add an onClick property to run open the modal
        <div className="AddButton btn-danger bg-gradient">
            +
        </div>
    );
}

export default AddButton;